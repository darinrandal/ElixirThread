<%= pagination = paginate @posts %>

<%= render 'posts', posts: @posts %>

<% if @posts.count == 0 %>
No posts were found :(
<% end %>

<%= pagination %>

<%= render 'footer_post' unless !user_signed_in? %>